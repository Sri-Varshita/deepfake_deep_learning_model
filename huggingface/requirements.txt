# Hugging Face Model Upload Requirements
# Install these packages to upload and manage models on Hugging Face Hub

# Core dependencies
tensorflow==2.15.1
huggingface-hub==0.24.7
numpy==1.26.4
pillow==10.4.0
opencv-python-headless==4.10.0.84

# Optional: For advanced features
# git-lfs  # Required for large file storage (install via system package manager)
